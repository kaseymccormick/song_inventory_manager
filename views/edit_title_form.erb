<form action="save_new_title">
  <h3>Edit an existing song</h3>
   <ul>Songs in your inventory are:
    <% Song.all.each do |song| %>
       <li> id: <%= song.song_id %> – <%= song.title %> – <%= song.artist %> genre: <%= song.genre_id %> status: <%= song.status_id %> desire level: <%= song.desire_level %>  </li>
    <% end %>
  </ul>
  <label for="id">What is the id of the song you'd like to edit</label>
      <input type="text" placeholder="3" name="id"></br>
  <label for="title">What is the new title?</label>
      <input type="text" placeholder="New Title" name="new_title"></br>
  <input type="submit">
</form>